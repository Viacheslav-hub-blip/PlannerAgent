# Integrations

Интеграционный слой для внешних сервисов, которые поставляют tools или данные, но сами
не являются реализацией LangChain tool внутри проекта.

- `mcp.py` подключает MCP-серверы через `MultiServerMCPClient` и мягко отключает MCP
  tools, если сервис недоступен.
